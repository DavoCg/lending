var common = require('./common');
var home = require('./home');
var box = require('./box');
var nav = require('./nav');

module.exports = {
    common: common,
    home: home,
    box: box,
    nav: nav
};