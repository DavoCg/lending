var navBar = require('./nav-bar');
var footer = require('./footer');
var select = require('./select');
var box = require('./box');

module.exports = {
    NavBar: navBar,
    ContentBox: box,
    Footer: footer,
    Select: select
};