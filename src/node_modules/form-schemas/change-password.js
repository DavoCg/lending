var validator =  require('validator');
var customValidator = require('custom-form-validator');

var changePasswordSchema = [
    {
        name: 'old',
        label: 'Current password',
        type: 'text',
        options: {type: 'password'},
        hasError: false,
        validators: [{fn: customValidator.isValidPassword, message: 'Should be long enough'}]
    },
    {
        name: 'password1',
        label: 'New password',
        type: 'text',
        options: {type: 'password'},
        hasError: false,
        validators: [{fn: customValidator.isValidPassword, message: 'Should be long enough'}]
    },
    {
        name: 'password2',
        label: 'Retype new password',
        type: 'text',
        options: {type: 'password'},
        hasError: false,
        validators: [{fn: customValidator.isValidPassword, message: 'Should be long enough'}]
    }
];

module.exports = changePasswordSchema;
